# Docker

This project demonstrate how to host a Web application inside a Docker container.

## How to run

| Description | Command |
| :--- | :--- |
| Build Docker image with WAR file inside | `make build` |
| Run a container hosting the application | `make run` |

> A file named `application.war` must be present in this folder
